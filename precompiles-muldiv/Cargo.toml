[package]
name = "precompiles-muldiv"
version = "0.1.0"
edition = "2024"

[dependencies]
array-concat = { version = "0.5.5", default-features = false }
bobcat-sdk = { version = "0.7.7", features = ["panic-revert", "ruint-enabled"] }
mini-alloc = "1.0.0"
